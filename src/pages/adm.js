import * as React from "react";
import Layout from "../components/layout";

const Admin = () => (
    <Layout>
        <section>
           
        </section>
    </Layout>
);

export default Admin;
